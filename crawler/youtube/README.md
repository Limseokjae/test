### Command
- 영상 & 자막 & 더보기 내용 다운로드
    ```
    $ python youtube.py --all 
    ```

- 영상만 다운로드
    ```
    $ python youtube.py --video
    ```

- 자막 & 더보기 내용 다운로드
    ```
    $ python youtube.py --meta
    ```
